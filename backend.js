export const BACKEND_URL = /*(window.location.href.indexOf("localhost") === -1)
    ? `${window.location.protocol.replace("http", "wss")}//${window.location.hostname}${(window.location.port && `:${window.location.port}`)}`
    : */
    "wss://gaf4cw.colyseus.de"
    //"ws://localhost:2567"


